const { session } = require("electron");
function verifyPin(host, fp) { return 0; }
session.defaultSession.setCertificateVerifyProc((request, callback) => {
  callback(verifyPin(request.hostname, request.certificate.fingerprint));
});
